# kog SNOMED translation resource manifest (generated by the project wizard).
# Mirrors configs/resources_ko.yaml (see it for the full kind / scope / key_path DSL).
# Minimal seed: a base style guide + exemplar retrieval over the bilingual pool.
# Add kog-specific term_dictionary / prompt_addendum / retrieval_corpus entries
# as SME-validated resources become available.

version: 1
language: kog

defaults:
  overlap:
    prompt_addendum: additive
    term_dictionary: most_specific
    retrieval_corpus: additive
    exemplar_set: union_dedupe

resources:

  - id: base_style_guide
    kind: prompt_addendum
    scope: "<<138875005"
    payload:
      path: style_guide/kog/style_guide_kog_seed.md

  - id: pool_exemplars
    kind: exemplar_set
    scope: "<<138875005"
    payload:
      source: kog_pool
